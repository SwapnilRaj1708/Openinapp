const Spinner = () => (
  <div
    style={{ borderTopColor: "var(--accent-color)" }}
    className="h-[20px] w-[20px] animate-spin rounded-full border-[4px] border-solid border-[var(--primary-color)]"
  ></div>
);

export default Spinner;
